import React from 'react'
import {View, Text} from 'react-native'

interface Props {}
const SongCategory = (props: Props) => {
    console.log(props)

    return <View></View>
}

export default SongCategory
