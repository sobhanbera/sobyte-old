import React from 'react'
import {View, Text} from 'react-native'

import {SongObject} from '../../interfaces'

interface Props {
    songs: Array<SongObject>
}
const CommonSongList = (props: Props) => {
    return <View></View>
}

export default CommonSongList
